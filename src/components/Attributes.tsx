import type { Player } from '../utils/data'
import { PageHead } from './PageHead'
export function Attributes({player}:{player:Player}){const icons=['🎯','⚔️','🎨','⚡','📚','🛡️'];const descriptions=['Precision','Consistency','Expression','Vitality','Learning','Recovery'];return <section className="sc"><div className="scr-scroll"><PageHead title="Attributes" sub="Your core powers shape every quest."/><div className="overall"><b>{player.level}</b><div><strong>Hero Level</strong><small>{player.xpInLevel} / {player.xpToNextLevel} XP to next level</small><div className="thin"><i style={{width:`${Math.min(100,player.xpInLevel/player.xpToNextLevel*100)}%`}}/></div></div></div><div className="agrid">{Object.entries(player.attributes).map(([name,v],i)=><div className="ac-card" key={name}><div className="air"><span>{icons[i%icons.length]}</span><b>Lv {v.level}</b></div><strong>{name}</strong><small>{descriptions[i%descriptions.length]}</small><div className="thin"><i style={{width:`${v.pct}%`}}/></div><small className="pct">{v.pct}%</small></div>)}</div></div></section>}
